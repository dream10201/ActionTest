name: Test Actions
on:
  release:
    types: [published]
  push:
    tags:
    - 'v*'
  watch:
    types: [started]

jobs:
  one:
    runs-on: ubuntu-latest
    env:
      ttt: hhhh
    steps:
      -
        name: checkout
        run:
          export ffs=${ffs}/fsdfsdf
          echo ${ffs}
        env:
          ffs: fffff 
      - name: fff
        run:
          echo $ttt
